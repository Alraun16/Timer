<Window x:Class="Timer.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Timer Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        SizeToContent="WidthAndHeight">

    <Border x:Name="OverlayBorder"
            Background="#00000000"
            CornerRadius="8"
            Padding="12,6">
        <Border.LayoutTransform>
            <ScaleTransform x:Name="OverlayScaleTransform"/>
        </Border.LayoutTransform>

        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Play/Pause" Click="PlayPauseMenuItem_Click"/>
                <MenuItem Header="Finish" Click="FinishMenuItem_Click"/>
                <MenuItem Header="Reset" Click="ResetMenuItem_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="ExitMenuItem_Click"/>
            </ContextMenu>
        </Border.ContextMenu>

        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="TimeText"
                       Text="00:05:00"
                       FontFamily="Courier New"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="White"
                       VerticalAlignment="Center"/>

            <Image x:Name="StateIcon"
                   Width="18"
                   Height="18"
                   Margin="8,-3,0,0"
                   RenderTransformOrigin="0.5,0.5"
                   VerticalAlignment="Center">
                <Image.RenderTransform>
                    <RotateTransform x:Name="StateIconRotate"/>
                </Image.RenderTransform>
            </Image>
        </StackPanel>
    </Border>
</Window>
